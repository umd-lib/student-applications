<%= simple_form_for @prospect,  html: { class: 'form-horizontal' },
  url: admin_prospect_path(@prospect),
  wrapper_mappings: {
    check_boxes: :horizontal_radio_and_checkboxes,
    radio_buttons: :vertical_radio_and_checkboxes,
    file: :horizontal_file_input,
    boolean: :horizontal_boolean
  } do |form| %>
  <%= form.error_notification %>
  <%= render 'prospects/id_and_semester_step', form: form %>
  <%= render 'prospects/contact_info_step', form: form %>
  <%= render 'prospects/work_experience_step', form: form %>
  <%= render 'prospects/skills_step', form: form %>
  <%= render 'prospects/availability_step', form: form %>
  <h3>Resume
    <% if @current_user %>
      <% if @current_user.admin %>
        <%= link_to "Change Resume", @resume ? edit_resume_path(@resume) : new_resume_path(@resume, prospect: @prospect.id), class: 'btn btn-info pull-right' %>
      <% end %>
    <% else %>
      <%= link_to "Change Resume", new_prospect_path(step: "upload_resume"), class: 'btn btn-info pull-right' %>
    <% end %>
  </h3>
  <div class="well">
    <%= link_to 'Back', admin_prospect_path(@prospect), class: 'btn btn-default' %>
    <%= form.submit 'Save', class: 'btn btn-success' %>
  </div>
<% end %>

<%= simple_form_for @prospect,  html: { class: 'form-horizontal' }, url: admin_prospect_path(@prospect) do |form| %>
  <%= render partial: "prospects/summary", locals: { form: form } %>
  <div class="well">
    <%= link_to 'Edit Submission', edit_admin_prospect_path(@prospect), class: 'btn btn-info' %>
  </div>
  <%= render(partial: "admin_section", locals: { form: form }) if @current_user.admin? %>
<% end %>
